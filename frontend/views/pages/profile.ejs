<% include('../layouts/header.ejs') %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/photo.css">

</head>
<body>
    
    
    <div class="container">
        <img src="<%= user.image %>" alt="Profile Image" class="profile-photo">
        <h3>Welcome to,<%= user.name %></h3>
     <!-- Display profile image -->
     <!-- <table>
        <tr>
            <td><img src="<%= user.image %>" alt="<%= user.image %>"></td>
        </tr>
     </table> -->
  
 <table>
    <tr>
        <td>Email</td>
        <td><%=user.email %></td>

    </tr>
    <tr>
        <td>Mobile Number:  </td>
        <td><%=user.mobile %></td>

    </tr>
   
    <tr>
        <td>Verified</td>
        <td>
            <% if(user.is_varified==1){
                %>
                <p>yes</p>
                <%
            }else{
                %>
                <p>no</p>
                <%
            }
            %>
        </td>

    </tr>
 </table>



<button><a href="/edit?id=<%=user._id %>">Edit</a></button>

<button><a href="/logout">Logout</a></button>
</div>
</body>
</html>

<% include('../layouts/footer.ejs') %>
